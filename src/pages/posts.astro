---
const response = await fetch("https://jsonplaceholder.typicode.com/posts");
const posts = await response.json();
---

<h1>My Posts!</h1>

{
    posts.map((post) => (
        <div>
            <h5>{post.userId}</h5>
            <h3 class="title">{post.title}</h3>
            <p style="color: #0c8888;">{post.body}</p>
        </div>
    ))
}

<style>
    .title{
        color: blueviolet;
    }
</style>